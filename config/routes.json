{
    "routes": [
      {
        "method": "GET",
        "path": "/admin",
        "handler": "Admin.serveAdminPanel",
        "config": {
          "policies": ["plugins::users-permissions.isAuthenticated"]
        }
      }
    ]
  }